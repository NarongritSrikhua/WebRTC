<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <script>
    const ROOM_ID = "<%= roomId %>"
  </script>
  <script defer src="https://unpkg.com/peerjs@1.2.0/dist/peerjs.min.js"></script>
  <script src="/socket.io/socket.io.js" defer></script>
  <script src="script.js" defer></script>
  <title>Document</title>
  <style>
    #video-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, 300px);
      grid-auto-rows: 300px;
    }
    
    video {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .controls {
      position: fixed;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      gap: 10px;
    }

    button {
      padding: 10px 20px;
      cursor: pointer;
    }

    #captures-section {
      margin-top: 20px;
      padding: 20px;
      border-top: 1px solid #ccc;
    }

    #captures-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, 300px);
      gap: 20px;
      margin-top: 20px;
    }

    .capture-container {
      position: relative;
      width: 100%;
    }

    .capture-container img {
      width: 100%;
      height: auto;
      max-width: 300px;
    }

    .capture-label {
      position: absolute;
      top: -25px;
      left: 0;
      font-size: 14px;
      color: #666;
      white-space: nowrap;
    }
  </style>
</head>
<body>
  <div id="video-grid"></div>
  <div class="controls">
    <button id="captureLocal">Capture My Video</button>
    <button id="captureRemote">Capture Remote Video</button>
  </div>
  <div id="captures-section">
    <h2>Captured Images</h2>
    <div id="captures-grid"></div>
  </div>
</body>
</html>
